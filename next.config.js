/** @type {import('next').NextConfig} */
const nextConfig = {
    // habilitando imagens
    images: {
        domains: ['storage.googleapis.com', 'img.icons8.com'],
    },
}

module.exports = nextConfig
